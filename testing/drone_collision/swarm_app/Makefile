# Path to your crazyflie-firmware clone.
# Default: ~/crazyflie-firmware in WSL (where it is already installed).
# Override at build time if needed:
#   make CRAZYFLIE_BASE=/other/path/crazyflie-firmware
CRAZYFLIE_BASE ?= $(HOME)/crazyflie-firmware

OOT_CONFIG := $(PWD)/app-config

include $(CRAZYFLIE_BASE)/tools/make/oot.mk
